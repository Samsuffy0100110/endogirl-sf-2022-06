{% extends 'base.html.twig' %}

{% block title %}Réinitialiser votre mot de passe{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
    <div class="container">
        <div class="row g-0">
            <div class="col-lg-8 mb-5">
                <img class="img-fluid" src="{{ asset('build/images/arrowHomeUp.png') }}" alt="Arrow">
            </div>
            <div class="col-lg-4">
                <h1 class="text-warning">Nouveau mot de passe</h1>
            </div>
        </div>
        <div class="row g-0">
            <div class="col-lg-4">
            </div>
            <div class="col-lg-4 bg-info p-5 mt-5 rounded shadow">
                {{ form_start(requestForm) }}
                {{ form_row(requestForm.email) }}
                <div>
                    <small>
                        Entrer votre email pour recevoir
                        un lien pour réinitialiser votre mot de passe.
                    </small>
                </div>
                <button class="btn btn-outline-warning mt-3">Envoyer</button>
                {{ form_end(requestForm) }}
            </div>
            <div class="col-lg-4">
            </div>
        </div>
        <div class="row mt-5 mb-5 g-0">
            <div class="col-lg-1">
            </div>
            <div class="col-lg-4">
            </div>
            <div class="col-lg-7">
                <img class="img-fluid" src="{{ asset('build/images/arrowdown.png') }}" alt="Arrow">
            </div>
        </div> 
    </div>
{% endblock %}
