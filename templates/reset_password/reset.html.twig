{% extends 'base.html.twig' %}

{% block title %}Redéfinir un mot de passe{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row g-0">
        <div class="col-lg-8 mb-5">
            <img class="img-fluid" src="{{ asset('build/images/arrowHomeUp.png') }}" alt="Arrow">
        </div>
        <div class="col-lg-4 mb-5">
            <h1 class="text-warning">Nouveau mot de passe</h1>
        </div>
    </div>
    <div class="row g-0 mb-5">
        <div class="col-lg-3">
        </div>
        <div class="col-lg-6 bg-info p-5 rounded shadow text-dark">
            {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword) }}
            <button class="btn btn-outline-warning">Enregistrer</button>
            {{ form_end(resetForm) }}
        </div>
        <div class="col-lg-3">
        </div>
    </div>
    <div class="row mt-5 mb-5 g-0">
        <div class="col-lg-1">
        </div>
        <div class="col-lg-4">
        </div>
        <div class="col-lg-7">
            <img class="img-fluid" src="{{ asset('build/images/arrowdown.png') }}" alt="Arrow">
        </div>
    </div>
</div>
{% endblock %}