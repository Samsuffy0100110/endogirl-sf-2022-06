{% if app.user %}
	<a href="{{ path('profile_dashboard')}}"><img style="border-radius: 50%" src="{{ ('/images/pictures/' ~ user.avatar)|imagine_filter('avatar') }}" alt="Register"></a>
{% endif %}
{% if app.user %}
	<p class="text-warning fw-bold">{{ app.user.nickname }}</p>
	<a data-no-swup href="{{ path('logout') }}">
		Se déconnecter</a>
{% else %}
	<a class="nav-link" href="{{ path('security')}}">Inscription/Connexion</a>
{% endif %}